if(typeof __config === "undefined") __config={};
module.exports = __config;
delete __config;

/* this uses the __config var set up in build.js
 * to receive load-time configuration from the firewall API.
 *
 */
